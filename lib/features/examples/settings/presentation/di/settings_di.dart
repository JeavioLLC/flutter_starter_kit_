import 'package:provider/single_child_widget.dart';

/// Registers settings feature dependencies.
/// Currently, the settings feature has no dependencies.
/// This function exists for consistency and future extensibility.
List<SingleChildWidget> registerSettingsFeature() {
  return [];
}
